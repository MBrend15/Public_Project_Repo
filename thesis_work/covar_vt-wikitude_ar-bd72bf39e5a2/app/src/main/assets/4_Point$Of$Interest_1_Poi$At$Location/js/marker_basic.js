//this is a function to handle multiple POIs. This function accepts a json object as a
//parameter and then uses the geo data to create a marker object which is then projected on a cam
 // as a Geo Object. You essentially need to pacakge everything into a contex, "this" and then
 //return that context to a collection of marker lists.
function Marker(poiData){

}